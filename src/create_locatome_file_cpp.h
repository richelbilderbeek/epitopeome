#ifndef CREATE_LOCATOME_FILE_CPP_H
#define CREATE_LOCATOME_FILE_CPP_H

#include <map>
#include <string>

/* Nope, use R function tmhmm::run_tmhmm

//' Creates a FASTA file with a locatome
//' @param proteome_filename file that contains a proteome in FASTA format
//' @param locatome_filename name of the locatome file to be created,
//'   saved in FASTA format
//' @return nothing
//' @author Richel J.C. Bilderbeek
// [[Rcpp::export]]
void create_locatome_file_cpp(
  const std::string& proteome_filename,
  const std::string& locatome_filename
);

*/

#endif // CREATE_LOCATOME_FILE_CPP_H



